import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Arborizacao_Inteligente',
  webDir: 'dist'
};

export default config;
